[project]
name = "kubecm"
version = "1.0.1"
description = "Easy Configuration Tools for Kubernetes Infrastructure engineers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "setuptools",
    "pydeep2"
]
